# Flatpaks.
#
# WARNING: FOUR BECAME ONE, matching the dwl session exactly. Spotify, Vesktop
# and Obsidian are applications somebody chooses; a window manager installing
# them is the same overreach that filled dnf-sysadmin.txt with nmap.
#
# OnlyOffice stays for the reason it stays in the dwl session: Fedora KDE ships
# no office suite at all, so its absence is a hole rather than a preference.
#
# Installed --user, not system-wide. A system Flatpak installed by one session
# would collide with the same app installed by the other.
org.onlyoffice.desktopeditors
