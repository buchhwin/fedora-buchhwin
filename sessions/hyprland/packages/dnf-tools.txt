# The themed toolbox.
#
# WARNING: SIX PACKAGES BECAME ONE, and the reason is the same one that emptied
# dnf-sysadmin.txt: this list existed because render.qml can write a colour
# scheme for these programs, which is a reason to THEME them, not a reason to
# INSTALL them. btop, bat, git-delta and tmux are things a person chooses; the
# desktop neither launches nor reads any of them.
#
# The theming stays. render.qml still writes their colours, so installing any
# of them later picks the palette up with no extra step:
#
#     dnf install btop bat git-delta tmux
#
# alacritty is gone for a different reason — it was a second terminal next to
# kitty, on a machine that already has Konsole from the KDE base. Three
# terminals is not a fallback, it is three terminals.
#
# Installed WITHOUT weak dependencies, like the desktop list.
fastfetch                   # the greeting in every new terminal (dotfiles/zsh/zshrc)
