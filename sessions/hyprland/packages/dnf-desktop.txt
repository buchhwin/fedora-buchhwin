# Buchhwin is an additional session on Fedora KDE Plasma. Plasma and SDDM are
# prerequisites (checked by lib/00-preflight.sh), not replaced.

# Compositor and shell.
hyprland
xdg-desktop-portal-hyprland
quickshell
qt6-qtdeclarative
# ⚠️ AND NOT `-devel`, WHICH THIS LIST CARRIED UNTIL 09.09.2026. That is a
# build-time package — headers and cmake files — and nothing in a running
# desktop opens either. Measured rather than reasoned about: the whole QML
# suite, all seventy checks, runs on a machine that has qt6-qtdeclarative
# and does NOT have the devel package. quickshell does not require it, and
# the dwl session never listed it.
kitty

# Fonts and small Wayland helpers used only by the Buchhwin session.
rsms-inter-vf-fonts
material-icons-fonts
google-noto-color-emoji-fonts
google-noto-sans-cjk-fonts
jetbrains-mono-fonts
breeze-cursor-theme
brightnessctl
ddcutil
cliphist
wl-clipboard
wtype
gammastep
pipewire-utils
sound-theme-freedesktop
grim
slurp
# Annotating a screenshot (Super+Ctrl+Print). The shell draws no editor of its
# own, so this is not a duplicate of anything — it is the same tool the dwl
# session uses, so both sessions annotate the same way.
swappy

# KDE is the only desktop integration layer. Do not add GNOME portals,
# keyrings, file managers, network applets or Bluetooth frontends here.
xdg-desktop-portal
xdg-desktop-portal-kde
polkit-kde
kwalletmanager
signon-kwallet-extension
kio-fuse
kio-gdrive
dolphin
okular
gwenview
kate
vlc
merkuro
akonadi
akonadi-server
kdepim-runtime
akonadi-calendar-tools
kaccounts-integration-qt6
kaccounts-providers
plasma-systemsettings

# Fedora KDE owns these facilities. The Buchhwin session opens KDE's tools
# instead of implementing a second Wi-Fi/Bluetooth UI.
bluez
plasma-nm
bluedevil

# Foreign application theming; generated config remains session-scoped.
breeze-gtk
papirus-icon-theme
qt6ct

# Removed, and each for a stated reason rather than to shorten the list:
#
#   wf-recorder   named nowhere in the tree except a checklist. Never started.
#   kcalc         services/Calculator.qml is the calculator; kcalc is unused.
#   plasma-pa     the Plasma volume applet needs plasmashell, which never runs
#                 in this session. Audio is services/Audio.qml over Pipewire.
#   powerdevil    KDE's idle, dim, lid and brightness daemon. The shell does all
#                 four itself, so this does not add a feature — it adds a second
#                 daemon competing for the backlight and the idle timer.
#   NetworkManager, udisks2
#                 both were listed here AND in dnf-core.txt.
