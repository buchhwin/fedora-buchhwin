# Applications. Skipped with --minimal.
brave-browser       # launched Wayland-native; see lib/60-shell.sh for the flags
code

# KDE/Qt applications used by the session.
gwenview                    # images (KDE)
vlc                         # video — Qt, and in Fedora's own updates repo
# No PDF viewer: the browser does it.
# Dolphin handles archives through KDE's integration.

# --- printing (his choice, 08.08.2026) --------------------------------------
# ⚠️ THIS ONE STARTS A SERVICE, which is why it was asked about rather than
# added quietly: cupsd listens on localhost:631. Everything else in these lists
# is a binary that does nothing until it is typed.
cups
cups-filters
plasma-discover

# --- java (he builds BuchhwinEconomy on this machine) ------------------------
java-latest-openjdk
java-latest-openjdk-devel
